const defaultState  ={}

let reducer = (state=defaultState,action) =>{

    switch (action.type) {
        case "value":
            
            break;
    
        default:
            break;
    }

    return state;
}

export default reducer;